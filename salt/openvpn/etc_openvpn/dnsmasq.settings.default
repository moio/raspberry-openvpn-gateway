# Use the 10.160.1.1 DNS server from inside the VPN to resolve hostnames in the vpndomain.com domain
# NOTE: rename this file to `dnsmasq.settings` before calling salt highstate
server=/vpndomain.com/10.160.1.1
